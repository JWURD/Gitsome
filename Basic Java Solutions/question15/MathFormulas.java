package question15;
/** This interface will define
 * addition, subtraction, multiplication, and division.
 * @author josh wurdemann
 * @version 8/20/2017
 *
 */
public interface MathFormulas {

	/**Adds numbers together to form a sum. */
	void addtion(int s, int x);

	/**Multiplies numbers together to form a product of combined numbers.*/
	void multiplcation(int s, int x);
	
	/**Divides to numbers together to form a quotient. */
	void division(int s, int x);
}
