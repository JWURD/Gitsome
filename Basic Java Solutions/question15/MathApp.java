package question15;

public class MathApp {

	public static void main(String[] args) {
		
		Math s = new Math(15, 32);
		s.addtion(s.getMyNum(), s.getMySomeNumber());
		s.multiplcation(s.getMyNum(), s.getMySomeNumber());
		s.division(s.getMyNum(), s.getMySomeNumber());
		
	}

}
